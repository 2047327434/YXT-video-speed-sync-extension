# 📚 云学堂自动挂课助手 (Chrome Extension)

一个专为 **云学堂（yunxuetang）** 设计的 Chrome 浏览器插件，支持**自动播放视频、视频结束后自动切换下一课、后台静默挂机、防检测机制**。

> ⚠️ **声明**：本插件仅供学习交流使用，请勿用于违反平台规则的场景。使用本插件产生的任何后果由使用者自行承担。

---

## ✨ 功能特性

| 功能 | 说明 |
|------|------|
| ▶️ **自动播放** | 页面加载后自动检测视频并播放，无需手动点击 |
| ⏭️ **自动切课** | 视频播放完毕后自动点击"下一个"，无缝切换下一节课 |
| 🔇 **后台静默** | 自动静音，视频在后台播放，不影响你做其他事情 |
| 🛡️ **失焦保活** | 页面切换到后台/标签页失焦时，视频不会被暂停 |
| 🎭 **反检测模拟** | 随机小暂停（1~3秒），模拟真实用户观看行为，绕过反作弊检测 |
| ❌ **弹窗自动关闭** | 自动关闭提示弹窗（不关闭考试、练习等关键弹窗） |
| 📊 **实时统计** | 显示运行时长、已完成课程数、当前视频进度 |
| 🔄 **状态持久化** | 页面刷新后自动恢复挂机，Popup 关闭不影响后台运行 |

---

## 📁 目录结构

```
YXT-auto-study-helper/
├── manifest.json        # 插件配置
├── content.js           # 核心挂机逻辑（注入页面）
├── background.js        # 后台服务
├── popup.html           # 控制面板
├── popup.css            # 面板样式
├── popup.js             # 面板交互
├── server/              # WebSocket 同步服务器（备用）
│   ├── package.json
│   └── sync-server.js
└── README.md
```

---

## 🚀 安装方法

1. 打开 Chrome，地址栏输入 `chrome://extensions/`
2. 右上角开启「**开发者模式**」
3. 点击「**加载已解压的扩展程序**」
4. 选择 `YXT-auto-study-helper` 文件夹

---

## 📖 使用教程

### 开始挂机

1. 打开云学堂课程页面（确保在视频播放页）
2. 点击浏览器工具栏的 📚 图标打开控制面板
3. 点击「**▶️ 开始挂机**」
4. 插件会自动：播放视频 → 视频结束 → 点击"下一个" → 播放下一课 → 循环

### 后台模式

- 勾选「**🔇 后台静默模式**」后，视频会自动静音
- 你可以切换到其他标签页或最小化浏览器，视频会继续播放
- 页面失焦时插件会自动保活，防止网站暂停视频

### 状态持久化（重要）

- **Popup 关闭不影响挂机**：点击插件外部或切换到其他页面，挂机仍在后台运行
- **页面刷新自动恢复**：如果云学堂页面刷新或跳转，插件会自动检测到之前的挂机状态并继续
- **手动停止**：再次打开插件面板，点击「⏹️ 停止挂机」才会真正停止

### 快捷操作

| 按钮 | 功能 |
|------|------|
| ⏭️ 跳过当前课 | 立即点击"下一个"，跳过当前正在播放的视频 |
| 🔄 刷新状态 | 手动刷新面板上的视频进度和状态 |

---

## 🛠️ 工作原理

```
页面加载
    │
    ▼
检测视频元素 ──否──► 等待2秒后重试
    │
    是
    ▼
自动播放视频（处理浏览器自动播放限制）
    │
    ▼
监听 video.ended 事件 ──或── 监听 timeupdate（currentTime >= duration - 3s）
    │
    ▼
等待 2~3 秒缓冲
    │
    ▼
点击"下一个"按钮
    │
    ▼
回到"检测视频元素"步骤，循环执行
```

### 防检测机制

| 机制 | 说明 |
|------|------|
| **失焦保活** | 覆盖 `visibilitychange` 事件，页面切换到后台时自动恢复播放 |
| **随机暂停** | 每 30~60 秒有 5% 概率暂停 1~3 秒，模拟真实用户偶尔离开 |
| **弹窗过滤** | 自动关闭提示类弹窗，但保留考试、练习、测评等关键弹窗 |

### 状态持久化机制

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   popup     │────►│ chrome.storage│◄────│ content.js  │
│  点击开始    │     │  (持久存储)   │     │  页面加载时  │
└─────────────┘     └─────────────┘     └─────────────┘
                           │                    │
                           ▼                    ▼
                    保存 enabled=true      读取并自动恢复挂机
```

---

## ⚠️ 注意事项

1. **请勿关闭标签页**：挂机期间请保持云学堂标签页打开，可以切换到其他标签页或最小化浏览器
2. **考试弹窗**：插件会自动识别并保留考试、练习、测评等关键弹窗，不会误关闭
3. **最后一课**：当找不到"下一个"按钮时，插件会自动停止并提示"所有课程已完成"
4. **网络问题**：如果视频加载失败或卡顿，插件会每 2 秒检测一次并自动重试播放
5. **状态恢复**：页面刷新后插件会自动恢复挂机，但如果关闭了整个浏览器窗口再重新打开，需要重新点击"开始挂机"

---

## 🔧 常见问题

**Q: 视频没有自动播放？**
> A: 部分浏览器会阻止自动播放。插件会尝试点击播放按钮，如果仍无法播放，请手动点击一次播放按钮，之后插件会接管。

**Q: 为什么视频播完后没有自动切课？**
> A: 检查"下一个"按钮是否在页面上可见。有些课程页面结构不同，插件可能无法定位按钮。可以手动点击"⏭️ 跳过当前课"。

**Q: 后台播放会被检测吗？**
> A: 插件内置了反检测机制，包括失焦保活和随机暂停模拟。但无法 100% 保证绕过所有检测，建议配合正常使用。

**Q: 点击插件外部后挂机停止了？**
> A: 请更新到最新版本（v2.1+）。现在插件使用 `chrome.storage` 持久化挂机状态，Popup 关闭后 content script 仍会继续运行，页面刷新也会自动恢复。

---

## 📝 更新日志

### v2.1 - 状态持久化
- 新增 `chrome.storage` 状态持久化，Popup 关闭/页面刷新后自动恢复挂机
- 修复点击插件外部后挂机中断的问题

### v2.0 - 自动挂课助手
- 重写为云学堂自动挂课助手
- 新增自动播放、自动切课、后台静默、失焦保活、反检测模拟、弹窗过滤
- 移除倍速播放功能（无法绕过后端校验）

### v1.0 - 视频倍速同步
- 初始版本，支持视频倍速播放和联网同步
- 云学堂倒计时倍速补丁

---

## 📄 开源协议

MIT License
